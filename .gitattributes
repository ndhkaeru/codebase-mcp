# Normalize text files to LF in both the repository and working tree.
* text=auto eol=lf

# Keep Windows-native script extensions on CRLF when they exist.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
